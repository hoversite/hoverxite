# Hoverxite — copy to .env.local
# Nothing here is required for the static landing page.

# Optional: LLM (BYOK) — used when agent features land
# OPENROUTER_API_KEY=
# AI_GATEWAY_API_KEY=

# Optional: scrape / import provider
# FIRECRAWL_API_KEY=
